rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 預約看屋：僅允許寫入（前端不需讀取列表，由後台或 Admin 讀取）
    match /viewingBookings/{bookingId} {
      allow create: if true;
      allow read, update, delete: if false;
    }
  }
}
